<div class="title_wrapper">
  <h2 class="heading_primary">welcome back, <%=@user.name%>!</h2>
  <h3> Your list of current favorites: </h3>
</div>

<%current_user.favorites.each do |favorite| %>

  <%@work = Work.find(favorite.work_id)%>
  <%= erb :"templates/import_sandbox"%>
<% end %>
